{% extends "base.html.twig" %}

{% block title %}{{ title }}
{% endblock %}

{% block content %}
  {# #}

  <code class="code">
    <h2>TEST BOX</h2>
    {{dump(playerHand)}}
    {{dump(dealerHand)}}
    {{dump(deck)}}
  </code>

  <div class="btn-container">
    <a href="{{path("game-home")}}" class="btn">
      Go back to game rules
    </a>
  </div>
{% endblock %}