{% extends "base.html.twig" %}

{% block title %}{{ title }}
{% endblock %}

{% block content %}
  <div class="site-wrap">
    <h1>{{ title }}</h1>

    <h3>Flow chart</h3>
    <img src="{{asset('img/flowchart.png')}}" alt="Flow chart over Black jack" class="img"/>
    <div class="btn-container">
      <a href="{{path("game-home")}}" class="btn">
        Go back to game rules
      </a>
    </div>
  </div>
{% endblock %}